# Base URL of the Novar FastAPI backend.
# Leave empty during local development — the Vite dev server proxies /upload,
# /chat, /sessions, and /health to http://localhost:8000.
# Set this in production deployments (e.g. https://your-api.onrender.com).
VITE_API_BASE_URL=